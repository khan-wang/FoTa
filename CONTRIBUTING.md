# Contributing

Issues and focused pull requests are welcome.

- Open an issue before making a large architectural change.
- Keep changes scoped and include the command used for verification.
- Do not commit datasets, checkpoints, experiment outputs, credentials, or
  machine-specific paths.
- Preserve third-party attribution when modifying derived files.
- For result changes, report the dataset, mask protocol, checkpoint hash, and
  evaluation command.

